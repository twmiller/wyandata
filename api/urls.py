# api/urls.py
from django.urls import path
from . import views

# Define your API URL patterns here
urlpatterns = [
    # For now, we'll add a placeholder that we can test later
    # path('test/', views.api_test, name='api_test'),
]